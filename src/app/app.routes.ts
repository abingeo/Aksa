import { Routes } from '@angular/router';
import { PhotoPopupComponent } from './components/photo-popup/photo-popup.component';

export const routes: Routes = [{ path: 'photo-popup', component: PhotoPopupComponent },];
